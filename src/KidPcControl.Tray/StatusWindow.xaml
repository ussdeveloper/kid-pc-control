<Window x:Class="KidPcControl.Tray.StatusWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Kid PC Control"
        Height="280" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
  <Grid Margin="20">
    <StackPanel>
      <TextBlock Text="Kid PC Control" FontSize="22" FontWeight="SemiBold"/>
      <TextBlock x:Name="DeviceNameText" Margin="0,8,0,0" FontSize="16"/>
      <TextBlock x:Name="StatusText" Margin="0,12,0,0" TextWrapping="Wrap"/>
      <TextBlock x:Name="UsageText" Margin="0,8,0,0" Foreground="{StaticResource MutedBrush}"/>
      <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
        <Button Style="{StaticResource PrimaryButton}" Content="Override…" Click="Override_Click" Margin="0,0,8,0"/>
        <Button Style="{StaticResource GhostButton}" Content="Odśwież" Click="Reload_Click"/>
      </StackPanel>
    </StackPanel>
  </Grid>
</Window>
